# LexAudit runs fully offline — no AI/LLM API key is required.
#
# Optional: override the browser origins allowed by CORS (comma-separated).
# ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
